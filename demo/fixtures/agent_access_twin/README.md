# Widget service (twin)

The twin widget service also caches computed results, but cache entries
expire after six hundred seconds. The two projects share file names, so this
fictional twin proves repository isolation.
