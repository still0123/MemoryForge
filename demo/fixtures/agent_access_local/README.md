# On-call rotation (local)

The on-call page number is 555-0199. This project is registered without the
public flag, so its facts are local_only and must never reach a model context
unless --allow-local-llm is set on the fixed server command.
