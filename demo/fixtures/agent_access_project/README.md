# Widget service

The widget service caches computed results before serving them. The constant
`cache_ttl_seconds` is set to 60, so cache entries expire after sixty seconds.
The retry policy is documented in docs/RETRY.md.

This fictional project is safe for public demos. No company systems, internal
links, customer data, or credentials are included.
